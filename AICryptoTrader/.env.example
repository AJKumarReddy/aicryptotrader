# Copy to .env.local and adjust. Never commit real keys.

# Clerk publishable key (safe in the browser by design).
VITE_CLERK_PUBLISHABLE_KEY=

# Base URL of the Python backend. Market data, sentiment and portfolio
# calls all go through it, so no provider key ships in this bundle.
VITE_API_URL=http://127.0.0.1:8000
